
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // ⚠️ تسمح لأي شخص في العالم بالقراءة والكتابة في جميع المجموعات
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
